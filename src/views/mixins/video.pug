mixin video(video)
    div
        h4
            a(href="/videos/" + video.id)=video.title
        ul
            li #{video.rating}/5 
            li #{video.comments} comments.
            li #{video.createdAt} created.
            li #{video.views} watched.
            li #{video.id}